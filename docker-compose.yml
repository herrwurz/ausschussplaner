services:
  api:
    build: .
    ports:
      - "8000:8000"
    environment:
      - DATABASE_URL=sqlite:////data/ausschussplaner.db
      - DEBUG=false
    volumes:
      - ./data:/data
    restart: unless-stopped
